/**
 * @internal/app
 * Alias for @internal/fastify-server
 *
 * Usage:
 *   import { logger, printRoutes } from "@internal/app";
 *   // equivalent to:
 *   import { logger, printRoutes } from "@internal/fastify-server";
 */

export * from "@internal/fastify-server";
