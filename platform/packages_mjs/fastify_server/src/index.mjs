/**
 * @internal/fastify_server
 * Alias for @internal/fastify-server (snake_case version)
 *
 * Usage:
 *   import { logger, printRoutes } from "@internal/fastify_server";
 *   // equivalent to:
 *   import { logger, printRoutes } from "@internal/fastify-server";
 */

export * from "@internal/fastify-server";
