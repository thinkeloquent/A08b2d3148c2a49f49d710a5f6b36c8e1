{
  "name": "@internal/{{APP_NAME}}-frontend-admin",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "fastify-apps/{{APP_NAME}}/frontend-admin/src",
  "projectType": "application",
  "tags": [
    "type:frontend-admin",
    "scope:fastify"
  ],
  "targets": {
    "lint": {},
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{projectRoot}/dist"
      ],
      "options": {
        "command": "npm run build",
        "cwd": "fastify-apps/{{APP_NAME}}/frontend-admin"
      }
    },
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run dev",
        "cwd": "fastify-apps/{{APP_NAME}}/frontend-admin"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run test",
        "cwd": "fastify-apps/{{APP_NAME}}/frontend-admin"
      }
    },
    "preview": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run preview",
        "cwd": "fastify-apps/{{APP_NAME}}/frontend-admin"
      }
    }
  }
}
