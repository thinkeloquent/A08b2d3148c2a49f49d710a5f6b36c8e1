import type { ReactNode } from 'react';

export interface {{APP_NAME_PASCAL}}Props {
  /** Content to render inside the component */
  children?: ReactNode;
  /** Additional CSS class names */
  className?: string;
}
