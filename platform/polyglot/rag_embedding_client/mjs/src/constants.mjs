/**
 * @fileoverview Shared constants for the embedding client subsystem.
 *
 * Re-exports from @internal/rag-embedding-config for backward compatibility.
 */

export { DEFAULT_BASE_URL, EMBEDDINGS_PATH, MAX_BATCH_SIZE } from '@internal/rag-embedding-config';
