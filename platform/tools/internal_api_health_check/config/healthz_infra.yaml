name: healthz_infra
description: Infrastructure health check endpoints

tests:
  - id: elasticsearch
    description: Elasticsearch health
    path: /healthz/elasticsearch
    expected_status: 200

  - id: postgres
    description: PostgreSQL health
    path: /healthz/postgres
    expected_status: 200

  - id: redis
    description: Redis health
    path: /healthz/redis
    expected_status: 200
