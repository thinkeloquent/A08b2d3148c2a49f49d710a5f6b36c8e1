/**
 * Protobuf re-exports
 *
 * Generated protobuf classes are available via the generated/ directory.
 * This file serves as the entry point for consuming the protobuf definitions.
 *
 * Run `pnpm run generate` to rebuild from .proto files.
 */

// Placeholder — import generated classes once protobuf generation is run
// import { fqdp } from '../generated/proto.js';
// export { fqdp };

export const PROTO_DIR = new URL("../proto", import.meta.url).pathname;
