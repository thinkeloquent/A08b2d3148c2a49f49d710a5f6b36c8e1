{
  "name": "categories",
  "version": "1.0.0",
  "description": "Category Manager - Fastify app with React frontend",
  "type": "module",
  "private": true,
  "scripts": {
    "app:dev": "node --watch backend/server.test.mjs",
    "app:start": "node backend/server.test.mjs",
    "test": "echo \"No tests yet\" && exit 0",
    "build": "pnpm --filter @internal/categories-frontend build"
  }
}
