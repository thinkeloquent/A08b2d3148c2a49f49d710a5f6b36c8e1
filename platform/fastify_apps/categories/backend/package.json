{
  "name": "@internal/fastify-app-categories",
  "version": "1.0.0",
  "description": "Category Manager Fastify backend plugin",
  "type": "module",
  "private": true,
  "main": "src/index.mjs",
  "scripts": {
    "app:dev": "node --watch server.test.mjs",
    "app:start": "node server.test.mjs",
    "test": "echo \"No tests yet\" && exit 0",
    "build": "echo \"No backend build required\" && exit 0"
  },
  "dependencies": {
    "@fastify/sensible": "^6.0.0",
    "@fastify/static": "^8.0.0",
    "dotenv": "^16.4.7",
    "fastify": "^5.0.0",
    "fastify-plugin": "^5.0.0",
    "sequelize": "^6.37.0"
  },
  "devDependencies": {
    "pino-pretty": "^13.0.0"
  }
}
